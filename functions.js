// This will create the first name for the user while they give infromation on the stray animal page  

const functions = {

      createuser: () => {

        const user = { firstname:'mahir' }
          user['lastname'] = 'chowdhury';
          return user;

    }    
};

    module.exports= functions;
